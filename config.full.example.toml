# Unicorn Viz — Full Example Configuration
#
# This is an exhaustive, documented template intended as a reference.
# For day-to-day use, keep config.toml minimal and only override what you need.

[window]
width = 1920
height = 1080
fullscreen = false
show_cursor = false   # true keeps pointer visible by default; Ctrl still reveals when false
title = "Unicorn Viz"
display_mode = "single"      # single | span_included | span_all | mirror_included | mirror_all
display_index = 0             # raw SDL display index used by single mode
exclude_display_indices = []  # raw SDL display indices ignored only by *_included modes

[demo]
# "sequential" or "random"
mode = "sequential"
# seconds per effect in auto-playlist mode
effect_duration = 20
# transition: crossfade, smoothfade, scanwipe_x, scanwipe_y, dissolve,
# zoomblend, or shuffle/random (pick transition each switch)
transition = "crossfade"
transition_duration = 1.0

[audio]
# empty string = auto-detect best source
device = ""
fft_bands = 512
buffer_seconds = 10.0
reactivity = 1.0
# low, medium, high
latency = "high"
# Silence gate (RMS).  Inputs below silence_rms_floor are treated as
# silent; the spectrum scales 0 → 1 over the next silence_rms_span of RMS.
# Raise these if quiet background noise registers as signal.
silence_rms_floor = 0.0060
silence_rms_span  = 0.045

[midi]
# empty string disables MIDI
# set a substring to match/open a specific input device
# Audio frequency response profile: house (default), trance, electronic, rap,
# hyphy, r&b, rock, generic, classical, ambient, pop, metal
profile = "house"
device = ""

[logging]
# DEBUG, INFO, WARNING, ERROR, CRITICAL
level = "INFO"
# directory for per-run log files
directory = "logs"

[dropins]
# Global drop-in exclusion list for dependency testing / support triage.
# Values:
# - "none" (default): enable all drop-ins
# - "all": disable all drop-ins
# - comma-separated drop-in names, e.g. "webcam-01,postfx-01"
exclude = "none"
safe_mode = false

[hotkeys]
# F6 randomizes effect speed within this range (when effect exposes `speed`)
random_speed_min = 0.25
random_speed_max = 2.50
# F7 randomizes audio reactivity within this range
random_reactivity_min = 0.40
random_reactivity_max = 2.00
# Alt+[/] adjusts zoom; Shift+Z randomizes within this range
zoom_min = 0.1
zoom_max = 3.0
random_zoom_min = 0.30
random_zoom_max = 1.80

[streaming]
enabled = false
auto_start = false
provider = "rumble"
endpoint = "rtmp://live-api-s.rumble.com/live"
stream_key = ""
rumble_endpoint = "rtmp://live-api-s.rumble.com/live"
youtube_endpoint = "rtmp://a.rtmp.youtube.com/live2"
custom_endpoint = "rtmp://your.rtmp.server/live"
# Optional full URL override (when set, endpoint/stream_key are ignored)
stream_url = ""
ffmpeg_path = "ffmpeg"
fps = 60
video_codec = "libx264"
preset = "veryfast"
pixel_format = "yuv420p"
include_audio = true
audio_input_format = "pulse"
audio_input_device = ""
audio_codec = "aac"
audio_bitrate = "160k"

[streaming.cta]
enabled = true
duration_s = 4.5
cooldown_s = 0.0
mode = "cycle"               # cycle | random
seed = 0
messages = [
	"Push the buttons!|👍",
	"Do the thangs!|🔔",
	"Share the love!|❤️ 📤",
]

[spotify]
enabled = false
player = "spotify"
poll_interval_s = 0.75
command_timeout_s = 0.25
now_playing_banner = true
now_playing_banner_hold_s = 10.0
# Optional path to local Spotify audio-features JSON map keyed by track id.
# features_file = "assets/audio/spotify_features.json"

[spotify.web_api]
enabled = false
client_id = ""
redirect_uri = "http://127.0.0.1:43879/callback"
scopes = [
	"user-read-playback-state",
	"user-read-currently-playing",
	"playlist-read-private",
	"playlist-read-collaborative",
]
token_store = "runtime/spotify-token.json"
poll_interval_s = 1.0
resolve_queue = true
resolve_playlist_context = true
# PKCE local flow does not require a client secret in runtime config.

[postfx]
enabled = true
active_slot = 0
hit_duration = 0.9
slot1_duration = 1.15
slot2_duration = 0.95
slot3_duration = 0.90
slot4_duration = 1.30
slot5_duration = 1.05
slot6_duration = 1.20
slot7_duration = 0.95
slot8_duration = 0.90
slot9_duration = 0.85
slot10_duration = 2.40
hue_shift_timeout_s = 3.0
hue_shift_step = 0.0277777778
rotation_timeout_s = 3.0
rotation_step_rad = 0.07

[grand_finale]
intensity = 1.0
blackout_s = 15.0

[candy_frame]
enabled = true
start_enabled = false
inset_px = 96.0
intensity = 1.0
pattern_interval_s = 120.0
reverse_on_cycle = true
outer_fill_mode = "auto"     # auto | mirror_blur | reactive_gradient | off
outer_fill_rotate_s = 24.0
outer_fill_mix = 0.84
seed = 0

[auto_vj]
enabled = false
beat_tracker_engine = "v2"   # v2 (default) | legacy
profile = "normie"   # chill | normie | raver | user
auto_profile_enabled = false
log_decisions = false
# Optional v2 analysis mode: short rolling beat-position map used for
# downbeat confidence gating and large tempo-lane shift validation.
# analysis_mode_enabled = true
# analysis_map_beats = 64
# analysis_region_min_beats = 8
# analysis_region_tol = 0.20
# analysis_region_confidence_min = 0.58
# analysis_downbeat_confidence_min = 0.55
# When auto_profile_enabled = false, explicit tuning keys below become manual
# USER mode layered over the chosen base preset.
# auto_profile_chill_max_bpm = 110.0
# auto_profile_raver_min_bpm = 136.0
# auto_profile_min_confidence = 0.45
# auto_profile_switch_cooldown_s = 20.0
# Optional override knobs (otherwise inherited from profile preset):
# allow_scroll_fx = true
# scrollfx_cruise_interval_s = 22.0
# scrollfx_climax_interval_s = 9.0
# scrollfx_rotation_chance = 0.50
# scrollfx_rotation_deg_min = 60.0
# scrollfx_rotation_deg_max = 120.0
# scrollfx_dy_min = 1
# scrollfx_dy_max = 3
# scrollfx_slew_steps_min = 4
# scrollfx_slew_steps_max = 8
# scrollfx_followup_s = 0.22
# Optional timed-finale support:
# show_duration_min = 90
# finale_auto_trigger = true
# finale_lead_s = 45.0
#
# Ping-pong controls:
# pingpong_a = "Tron Grid"     # manual hotkey slot A default
# pingpong_b = "Hacker Terminal"  # manual hotkey slot B default
# pingpong_beats = 4            # beats between ping-pong swaps
# pingpong_auto_enabled = false # auto mode is intended for raver only
# pingpong_auto_bpm_min = 138.0
# pingpong_auto_trigger_chance = 0.35
# pingpong_auto_min_swaps = 4
# pingpong_auto_max_swaps = 10

[control_room]
enabled = false
display_index = 1
width = 1440
height = 900
show_preview = true
preview_scale = 0.52
theme = "dark"   # dark | high_contrast

[ansi]
# default ANSI art directory
ansi_dir_auto = "assets/ansi"

[playlist]
# class name or display name; empty = first discovered effect
start_effect = "Audio Spectrum"
# explicit sequence; empty = all effects
sequence = []

# ------------------------------------------------------------
# Per-effect overrides (class-name keys)
# ------------------------------------------------------------
[effects.AlienBiome]
speed = 1.0
# Optional: constrain the global random-speed hotkey while this effect is active.
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.ANSIViewer]
speed = 1.0
glow = 0.6
crt = 0.7
slide_time = 15.0
cycle_files = 0.0

[effects.AudioSpectrum]
mode = 2
glow = 1.0
# scale_when_framed = true
# candy_frame_disallow = false

[effects.CopperBars]
speed = 1.0

[effects.Cosmos]
speed = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.Cube3D]
speed = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.Dali]
speed = 1.0
rotation_interval = 15.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.Escher]
speed = 1.0
vignette = 0.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.Curtains]
intensity = 0.82
speed = 1.0
zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_zoom_min = 0.85
# random_zoom_max = 1.15

[effects.Fire]
intensity = 0.90
speed = 1.0
# zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.35
# random_zoom_min = 0.85
# random_zoom_max = 1.15

[effects.FractalZoom]
speed = 1.0
max_iter = 180
# random_speed_min = 0.70
# random_speed_max = 1.20

[effects.Metaballs]
speed = 1.0
# zoom = 1.0           # override rng startup zoom; unset = random each run
# random_speed_min = 0.75
# random_speed_max = 1.25
# random_zoom_min = 0.70
# random_zoom_max = 1.30

[effects.ParticleStorm]
speed = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.Plasma]
speed = 1.0
palette = 0.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.SineScroller]
speed = 1.5
amplitude = 0.18
font_scale = 4.0
# random_speed_min = 1.10
# random_speed_max = 1.90

[effects.SimShowcase]
speed = 1.0
mix_time = 14.0
camera_energy = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_reactivity_min = 0.80
# random_reactivity_max = 1.10
# Single override directory (legacy):
# scene_dir = "drop-ins/sims-01/scenes"
# Multi-source directories (comma-separated). Useful for public robotics
# training datasets exported as .usda/.usdc/.usdz:
# scene_dirs = "drop-ins/sims-01/scenes,assets/robotics-usd,assets/sims"

[effects.ProjectMEffect]
speed = 1.0
preset_duration = 20.0
smooth_transition = true
lock_preset = false
start_clean = false
beat_sensitivity = 1.0
fps_hint = 60
projectm_library = ""
# random_speed_min = 0.80
# random_speed_max = 1.20
# Single override directory:
# preset_dir = "drop-ins/projectm-01/presets"
# Multiple preset packs (comma-separated):
# preset_dirs = "drop-ins/projectm-01/presets,/opt/projectm/presets"
# Optional texture search paths for presets that reference images:
# texture_dirs = "assets/projectm-textures"
# Optional startup preset file name or full path:
# start_preset = "Geiss - Feedback Spiral.milk"

[effects.Kaleidoscope]
speed    = 1.0
segments = 12.0    # mirror-image sectors: 6=wide triangles, 12=hex star, 18=tight circle
zoom     = 0.62    # zoom into source material (0.3 fine detail – 2.0 broad forms)
# random_speed_min = 0.70
# random_speed_max = 1.40
# random_zoom_min = 0.45
# random_zoom_max = 0.90

[effects.Starfield]
speed = 0.5
warp = 0.0
# random_speed_min = 0.35
# random_speed_max = 0.75

[effects.Tunnel]
speed = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.VanGogh]
speed = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.Vector]
speed = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25

[effects.CrystalPyramids]
speed = 1.0
# random_speed_min = 0.70
# random_speed_max = 1.30

[effects.Fireworks]
speed = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20

[effects.Psychedelic]
speed = 1.0
# random_speed_min = 0.70
# random_speed_max = 1.30

# ------------------------------------------------------------
# Drop-in effect overrides
# ------------------------------------------------------------

[effects.AlienInvasion]
fleet_speed = 1.2
zoom = 1.0
# random_zoom_min = 0.70
# random_zoom_max = 1.30

[effects.CyberWar]
attack_speed = 2.0
zoom = 1.0
# random_zoom_min = 0.70
# random_zoom_max = 1.30

[effects.DiscoBall]
speed = 1.0
rotation_speed = 1.0
num_tiles_u = 16
num_tiles_v = 12
laser_intensity = 1.0
zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_zoom_min = 0.85
# random_zoom_max = 1.15

[effects.HackerTerminal]
text_speed = 80.0
zoom = 1.0
# random_zoom_min = 0.80
# random_zoom_max = 1.20

[effects.ImageShowcase]
speed = 1.0
mix_time = 10.0
zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_zoom_min = 0.85
# random_zoom_max = 1.15

[effects.TextureShowcase]
speed = 1.0
mix_time = 7.0
zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_zoom_min = 0.85
# random_zoom_max = 1.15

[effects.TronGrid]
speed = 1.0
grid_scale = 1.0
num_beams = 6
zoom = 1.0
# random_speed_min = 0.75
# random_speed_max = 1.25
# random_zoom_min = 0.80
# random_zoom_max = 1.20

[effects.UnicornTears]
speed = 1.0
zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_zoom_min = 0.85
# random_zoom_max = 1.15

[effects.VideoShowcase]
speed = 1.0
mix_time = 10.0
zoom = 1.0
# random_speed_min = 0.80
# random_speed_max = 1.20
# random_zoom_min = 0.85
# random_zoom_max = 1.15

# ── projectM affinity & extended dwell ──────────────────────────────────────
# Controls how often the Auto VJ biases toward ProjectM Presets and how long
# it lingers there.  These keys live inside [auto_vj] and respect per-profile
# overrides.
#
# projectm_affinity = 0.30              # 0 = disabled; 1 = always pick projectM
# projectm_dwell_mult = 2.0             # multiply dwell timer when projectM wins
# projectm_preset_min_interval_s = 6.0  # min secs between Auto-VJ preset changes
# projectm_preset_on_event_chance = 0.75 # chance of random preset on drop/impact/etc

# ── Preset-level ping-pong inside projectM (raver only) ─────────────────────
# When enabled, the Auto VJ bounces between two random projectM presets for a
# run of N beats instead of swapping whole effects.
#
# projectm_preset_pingpong_enabled = true
# projectm_preset_pingpong_beats = 4
# projectm_preset_pingpong_min_swaps = 4
# projectm_preset_pingpong_max_swaps = 10

# ── Post-FX ping-pong (raver only, layered) ──────────────────────────────────
# Accelerating oscillator: fires slot A at full natural duration, then B as A
# concludes, each subsequent swap shorter by decay factor until min_interval is
# reached, then finale plays the last slot at full duration and exits.
# Runs simultaneously with effect/preset ping-pong.
#
# postfx_pingpong_enabled = true
# postfx_pingpong_decay = 0.68           # per-swap multiplier; lower = faster
# postfx_pingpong_min_interval_s = 0.12  # floor (s) — finale fires at this point
# postfx_pingpong_trigger_chance = 0.40
