moderngl>=5.10
pysdl2>=0.9.16
pysdl2-dll>=2.28
numpy>=1.26
scipy>=1.12
sounddevice>=0.4.6
python-rtmidi>=1.5
Pillow>=10.0
psutil>=5.9
opencv-python-headless>=4.9   # camera overlay + WebcamOverlay effect
# Optional — required only for LLM detector scoring in tools/package_training_set.py.
# Install whichever provider you have an API key for.
openai>=1.30                  # set OPENAI_API_KEY in environment
anthropic>=0.28               # set ANTHROPIC_API_KEY in environment
